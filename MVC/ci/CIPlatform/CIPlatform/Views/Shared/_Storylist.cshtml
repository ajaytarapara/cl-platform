@model CIPlatform.Entities.ViewModels.PaginationMission
 @foreach (var Item in Model.Stories)
{

    if (@Item.storystatus == "approved")
    {
        <div class="card mb-5 mx-3" style="width: 25rem;height: 486px;display: flex;">
            @if (@Item.storymediapath != null)
            {
                <div class="howerimg" style="">
                    @{
                        string[] mediapath = @Item.storymediapath.ToString().Split(",");
                    }
                    <img src="~/images/uploads/@mediapath[0]" alt="Avatar" class="howermainimage" style="width: 106%; margin-left: -11px;height:198px;">
                    @*<img src="~/images/uploads/@mediapath[0]" alt="Avatar" class="howermainimage" style="width: 106%; margin-left: -11px;">*@
                    <div class="overlay overlayforhower">
                        <div class="howertext">
                            <button id="btnhower">
                                <a href="@Url.Action("View_Story", "Story", new { storyid = @Item.storyid })"
                       style="color:white;text-decoration:none;">View details</a>
                                <i class="bi bi-arrow-right" style="color:white"></i>
                            </button>
                        </div>
                    </div>
                    <div class="storytheme">
                        <spam class="storyth">@Item.themetitle</spam>
                    </div>
                </div>
            }
            <div class="card-body" style="height:202px;overflow:hidden">
                <h5 class="card-title" style="margin-top: 5%;">
                    @Item.storytitle
                </h5>
                <p style="color:#6c757d">
                    @{
                        string description = @Item.storydescription.ToString().Replace("<p>", "");
                        description = description.Replace("</p>", "");
                        description = description.Replace("<strong>", "");
                        description = description.Replace("</strong>", "");
                    }
                    @description
                </p>
            </div>
            <div class="card-body">
                <img src="@Item.useravtar" class="storycardimg" />&nbsp;<span id="storycardimgtext">
                    @Item.userfirstname @Item.userlastname <br>
                    <span style="margin-left:70px"> @Item.storypublishdate.ToString().Substring(0,10)</span>
                </span>

            </div>
        </div>
    }
}
<div class="Pagination d-flex mt-5" style="justify-content:center;">
    <nav aria-label="Page navigation example">
        <ul class="pagination justify-content-center" id="pagination" style="display:flex;">
            @for (int i = 1; i <= Model.pageCount; i++)
            {
                <li class="page-item d-flex">
                    <a class="page-link" style="background-color:orange;opacity:0.7;margin-right:10px;" href="">@i</a>
                </li>
            }
        </ul>
    </nav>
</div>



